---
case_id: NBI-022
status: FAIL
persona: "FREE-READY, PLUS-ACTIVE"
bd_refs: "Nabi §5.4, §16; Nabi AC-05..07, AC-15"
entrypoint: lib/main.dart
build: "debug source audit at 9e81b08b9ae2aab4e1c263f99b044002059b4bb0"
device: "Not executed: the unified runtime does not mount an M30 surface"
run_id: FP-20260728-NBI-RUNTIME-AUDIT
command_id: CMD-20260728-NBI-RUNTIME-001
technical_evidence: technical/NBI-M30-runtime-audit.md
actual_result: "FAIL — no M30 bubble/CTA is mounted and the configured navigation gateway returns false."
---

# NBI-022 — Nabi tap, CTA deep link and back navigation

## Persona and preconditions

`FREE-READY` and `PLUS-ACTIVE`; both use the same unified entry point. This
audit does not substitute the legacy V1 mascot for the M30 bubble.

## Steps

1. Inspect the M30 shell's use sites from the active app.
2. Inspect primary/secondary CTA handling in the controller.
3. Inspect the default navigation gateway result.

## Expected

Tapping Nabi or its CTA deep-links to the permitted destination and Back
preserves the originating context without an entitlement bypass.

## Actual and decision

No M30 bubble is installed. If controller CTA code were reached, the default
gateway returns `false`; no live deep link or back-navigation behaviour exists.
Recorded **FAIL**.

See `technical/NBI-M30-runtime-audit.md` and the linked M30 finding.
