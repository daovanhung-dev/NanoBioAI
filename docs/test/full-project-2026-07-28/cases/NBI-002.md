---
case_id: NBI-002
status: FAIL
persona: FREE-EXHAUSTED
bd_refs: "Nabi NBI-FREE-002; Nabi AC-01..05, AC-15; Product Flow M06, M07"
entrypoint: lib/main.dart
build: "debug source audit at 9e81b08b9ae2aab4e1c263f99b044002059b4bb0"
device: "Not executed: the unified runtime does not mount an M30 surface"
run_id: FP-20260728-NBI-RUNTIME-AUDIT
command_id: CMD-20260728-NBI-RUNTIME-001
technical_evidence: technical/NBI-M30-runtime-audit.md
actual_result: "FAIL — the NBI-FREE-002 quota warning/exhaustion flow has no reachable M30 trigger, surface, or CTA."
---

# NBI-002 — Nabi question quota warning or exhaustion

## Persona and preconditions

`FREE-EXHAUSTED` with Product Flow M06/M07 quota semantics. The catalog has
`NBI-FREE-002`, while the integrated app has no M30 lifecycle hook.

## Steps

1. Audit catalog, controller-provider reachability and `lib/main.dart` startup.
2. Audit the rendering shell and CTA dependencies.
3. Look for a safe runtime path from a quota event to this notification.

## Expected

Quota warning/exhaustion is shown only when eligible, respects suppression and
cooldown, and sends the user through its allowed CTA without changing quota.

## Actual and decision

There is no application-level caller for `evaluate` and no M30 shell; the
quota event cannot create a user-visible notification or CTA. Recorded **FAIL**
on integration evidence, not on older package terminology.

See `technical/NBI-M30-runtime-audit.md` and the linked M30 finding.
