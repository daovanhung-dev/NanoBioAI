# Logbug 14-7-26

## Mục tiêu

Khóa các lỗi liên quan đến tạo lịch chồng lấn, thời gian lịch không theo nhịp sinh hoạt, biên hoàn thành nhiệm vụ, camera proof, cấu hình AI Chat và quota commit.

## Tiến độ theo trọng số

| Nhóm | Trọng số | Đạt | Evidence |
|---|---:|---:|---|
| DD/contract M01, M02, M03, M07, M09 | 50% | 50% | Năm implementation delta ngày 2026-07-15, changelog, BD wellness boundary và Supabase contract đã đồng bộ. |
| Runtime/tooling/SQL source | 30% | 30% | Horizon gate, single-flight/idempotency, daily routine/resolver, completion inclusive, camera recheck, AI fail-closed, quota retry, runtime defines và SQL 10/11 item đã triển khai. |
| Verification/acceptance | 20% | 10% | `flutter analyze` sạch; các targeted bundle và SQL/tool contracts pass. Chưa có Supabase sandbox/project ref/CLI; full suite còn baseline failures ngoài phạm vi. |
| **Tổng** | **100%** | **90%** | Chưa được claim production acceptance. |

## Checklist thực hiện

- [x] Cập nhật DD trước khi chốt implementation.
- [x] Horizon lấy max final date từ `meal_plans` và `lifestyle_schedule_items`, inclusive theo Việt Nam, malformed fail closed.
- [x] Retry succeeded trước gate; generation mới gate trước quota/AI; single-flight theo user.
- [x] Dashboard dùng cùng horizon và disable CTA khi còn ít nhất hai ngày.
- [x] Daily routine weekday/weekend versioned `daily_routine_v1`, editor onboarding/legacy/settings/schedule và resolver sở hữu thời gian.
- [x] Timeline có nap item thật; SQL manifest chấp nhận 10/11 item/ngày.
- [x] Completion/undo inclusive đến đúng `+30 phút`; camera bắt buộc và recheck sau capture.
- [x] AI Chat không trả technical fallback như AI success; typed failure và quota commit retry ba lần cùng request id.
- [x] IDE/run/build dùng `--dart-define-from-file`; không bundle dotenv.
- [x] Targeted tests và full analyzer.
- [ ] Xác minh/apply migration 16 trên Supabase sandbox thật, RLS/Storage/concurrency/two-user smoke.
- [ ] Đưa full suite về xanh sau khi xử lý các baseline failure ngoài phạm vi và file localization đang do người dùng chỉnh.

## Blocker production

Workspace không có `supabase/config.toml` và không có Supabase CLI, nên không thể xác minh project ref. Theo safety gate, không chạy `config.sql` destructive và không apply migration từ xa. `wellness_rewards_rollout.enabled` phải tiếp tục giữ `false`.
