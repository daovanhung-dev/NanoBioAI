import 'package:flutter/material.dart';

class AppShadows {
  AppShadows._();

  static List<BoxShadow> primaryShadow = [
    BoxShadow(
      color: Colors.black12,
      blurRadius: 12,
      offset: Offset(0, 4),
    ),
  ];
}
