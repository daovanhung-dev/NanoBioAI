# Component Design System

Build from foundation → semantic tokens → primitives → medical compositions → feature UI. Components own consistent states: enabled, pressed, focused, disabled, loading, selected and error where relevant. Avoid feature-local copies of buttons, cards, status pills and state containers when the semantic primitive already exists. See `components/`.
