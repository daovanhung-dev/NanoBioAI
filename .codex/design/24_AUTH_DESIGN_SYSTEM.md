# Auth Design System

Single-column trust layout, clear field labels, autofill support, inline validation, password visibility, backend-unavailable state and unambiguous route prompts. Security copy avoids revealing account existence where not necessary. Auth redirects must not flash protected UI.
