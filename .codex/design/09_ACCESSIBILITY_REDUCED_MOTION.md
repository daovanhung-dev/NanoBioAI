# Accessibility & Reduced Motion

Support system text scaling, screen readers, 48 dp targets, semantic labels, sufficient contrast and non-color state encoding. Test narrow width + large text together. Reduce Motion removes large translation, scale, parallax and ambient loops. Reduce Transparency replaces glass with opaque tonal surface. High contrast increases text/border separation without changing semantic meaning.
