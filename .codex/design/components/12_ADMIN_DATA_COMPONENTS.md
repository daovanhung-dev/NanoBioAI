# 12 Admin Data Components

Admin components include metric tile, filter/search bar, data row/table, status badge, permission state, review queue and mutation confirmation. Density never removes semantics or focus visibility.
