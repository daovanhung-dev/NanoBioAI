# 03 Chips Filters

Chips are compact selection/filter/status controls. Multi-select uses selected shape/indicator plus semantic label. Exclusive “none” choices must clear conflicting selections through existing controller logic.
