# 01 Buttons

Buttons: one primary action per task region; filled for commit, tonal/outlined for secondary, text for tertiary. Loading replaces icon without changing width. Disabled requires contextual explanation when non-obvious.
