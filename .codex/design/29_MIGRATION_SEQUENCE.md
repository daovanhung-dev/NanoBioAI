# Migration Sequence

Migrate foundation before feature pages. Do not introduce new visual values in a page while a canonical token can express the same role. Keep old public theme symbols as compatibility facades until callers are migrated and tests prove no duplicate contract. Complete one feature family at a time so visual drift is reviewable.
