# Design Token Mapping

Implementation maps specs to existing semantic APIs such as `AppColors`, `AppSpacing`, `AppRadius`, `AppTextStyles`, `AppGradients`, `AppShadows`, `AppDuration`, motion scope and medical primitives. New raw literals require justification and should normally become semantic tokens first.
